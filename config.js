// Extension configuration
const CONFIG = {
    // Maximum length for generated summary (in tokens)
    MAX_SUMMARY_LENGTH: 150,
    // Minimum length for generated summary (in tokens)
    MIN_SUMMARY_LENGTH: 50,
    // Maximum length of input text to summarize (in characters)
    MAX_INPUT_LENGTH: 1024,
    // Cache settings
    CACHE_CLEAR_INTERVAL: 30 * 60 * 1000,  // 30 minutes in milliseconds
};
